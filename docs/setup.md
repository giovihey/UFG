# Setup

Some practical tips for setup

## Docs setup

To run the docs locally 

1. build a python virtual environment:
```Bash
python3 -m venv .venv && source .venv/bin/activate
```

2. install the requirements with
```Bash
pip install -r requirements.txt  
```

3. run the command
```Bash
mkdocs serve
```


